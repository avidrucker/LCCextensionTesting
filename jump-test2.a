        mov r0, 5
        ; jmp r0-1 ; DONE: fix highlighting so that the -1 here isn't highlighted as valid code
        jmp r0, 1
        dout
        dout
        dout
        dout
        dout
        halt