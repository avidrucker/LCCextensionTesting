        mov r0, 'a'
        aout r0,
        nl
        ld r0, x 
        aout r0
        nl        
        halt

x:        .word 'bc' ; TODO: LCC should report this as an error (invalid char), but it does not